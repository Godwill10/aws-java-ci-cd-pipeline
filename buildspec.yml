version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  build:
    commands:
      - echo "Building Java project..."
      - javac HelloWorld.java

artifacts:
  files:
    - '**/*'